(specifications->manifest
 '("coreutils"
   "guile"
   "guile-json" ;; may not need at all
   "guile-sqlite3"
   "guile-ini"

   ;; TODO maybe use guile? IDK.
   "python"
   "python-paho-mqtt"
   ;; not used.
   "python-sexpdata"))
